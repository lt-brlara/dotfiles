# [Created by task 2.6.2 7/16/2022 11:20:23]
# Taskwarrior program configuration file.
# For more documentation, see https://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# You can also refence environment variables:
#   variable=$HOME/task
#   variable=$VALUE

# Use the command 'task show' to see all defaults and overrides

# Files
#data.location=/Users/blara/.task

# To use the default location of the XDG directories,
# move this configuration file from ~/.taskrc to ~/.config/task/taskrc and uncomment below

data.location=~/.local/share/task
hooks.location=~/.config/task/hooks

# Color theme (uncomment one to use)

_forcecolor = on 

include dark-256.theme


# Taskwarrior Server - Bayla-01
taskd.ca          = ~/.config/task/ca.cert.pem
taskd.key         = ~/.config/task/benjamin_lara.key.pem
taskd.certificate = ~/.config/task/benjamin_lara.cert.pem

taskd.credentials = Main/Benjamin Lara/fc67ec1b-ee59-401e-b8ac-efd94cdb8f3a
taskd.server = 192.168.118.68:53589

taskd.trust = ignore hostname

# Contexts

context.mantech   = project.not:laratechs or project:mantech or +in 
context.laratechs = project.not:mantech or project:laratechs +next or +in 

# UDAs
uda.brainpower.type     = string
uda.brainpower.label    = Brainpower
uda.brainpower.values   = H,M,L
uda.brainpower.default  = M

uda.estimate.type   = numeric
uda.estimate.label  = Estimate

# Custom reports
report.next.description     = Most urgent tasks
report.next.columns         = id,start.age,entry.age,urgency,depends,priority,project,tags,recur,scheduled.countdown,due.remaining,until.remaining,description,brainpower
report.next.filter          = status:pending and +next
report.next.labels          = ID,Active,Age,Urgency,Deps,P,Project,Tag,Recur,S,Due,Until,Description,Brainpower
report.next.sort            = brainpower-,priority-,urgency-

# GTD Configs
urgency.user.tag.in.coefficient = 15.0

report.in.columns     = id,description
report.in.description = Inbox
report.in.filter      = status:pending limit:page (+in)
report.in.labels      = ID,Description
